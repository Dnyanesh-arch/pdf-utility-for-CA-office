{
  "name": "ca-pdf-utility-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/desktop",
    "build": "npm run build -w apps/desktop",
    "lint": "npm run lint -w apps/desktop",
    "test": "npm run test -w apps/desktop"
  }
}
