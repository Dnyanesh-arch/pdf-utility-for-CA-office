export function Dashboard() {
  return (
    <section>
      <h2>Dashboard</h2>
      <p>Local-first utility for CA office PDF workflows.</p>
      <ul>
        <li>Merge PDFs with optional auto index page.</li>
        <li>Compress PDFs with profile-driven settings.</li>
        <li>Split PDFs by range, fixed pages, and max size.</li>
      </ul>
    </section>
  );
}
